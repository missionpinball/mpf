#config_version=5
mode:
  start_events: start_no_virtual

multiball_locks:
    lock_no_virtual:
        lock_devices: bd_lock
        balls_to_lock: 2
        locked_ball_counting_strategy: no_virtual
        debug: True
