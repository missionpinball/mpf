#config_version=6

hardware:
    platform: fast

machine_vars:
  master_volume:
    initial_value: 10
    value_type: int
    persist: true

fast:
  aud:
    debug: true
    port: com9
    baud: 230400
